import React from 'react';
import { Supplier } from '../../types';
import { BadgeCheck, Star, MapPin, Package, Clock, ShieldCheck, CheckCircle2 } from 'lucide-react';

interface SupplierCardProps {
  supplier: Supplier;
}

export const SupplierCard: React.FC<SupplierCardProps> = ({ supplier }) => {


  const typeColorMap = {
    Importer: 'bg-indigo-50 text-indigo-800 border-indigo-200',
    Distributor: 'bg-blue-50 text-blue-800 border-blue-200',
    Retailer: 'bg-emerald-50 text-emerald-800 border-emerald-200',
    Manufacturer: 'bg-amber-50 text-amber-800 border-amber-200'
  };

  return (
    <div className="bg-white rounded-2xl border border-slate-200/90 shadow-xs hover:shadow-md hover:border-slate-300 transition-all p-6 sm:p-7 flex flex-col justify-between">
      <div>
        <div className="flex items-start justify-between gap-3">
          <div>
            <div className="flex items-center gap-2 flex-wrap">
              <span className={`text-[11px] font-bold px-2.5 py-0.5 rounded-full border ${typeColorMap[supplier.type]}`}>
                {supplier.type}
              </span>
              {supplier.verified && (
                <span className="inline-flex items-center gap-1 text-[11px] font-bold px-2.5 py-0.5 rounded-full bg-emerald-50 text-emerald-800 border border-emerald-200">
                  <BadgeCheck size={12} className="text-emerald-600" />
                  Verified Supplier
                </span>
              )}
            </div>
            <h3 className="mt-2.5 text-lg font-bold text-slate-900 leading-snug">
              {supplier.name}
            </h3>
          </div>
          <div className="flex items-center gap-1 bg-amber-50 px-2 py-1 rounded-lg border border-amber-200/80 text-amber-900 text-xs font-bold shrink-0">
            <Star size={13} className="fill-amber-400 text-amber-500" />
            <span>{supplier.rating.toFixed(1)}</span>
            <span className="text-[10px] text-amber-700 font-normal">({supplier.reviewCount})</span>
          </div>
        </div>

        <p className="mt-3 text-xs text-slate-600 line-clamp-2">
          {supplier.description}
        </p>

        <div className="mt-4 pt-3.5 border-t border-slate-100 grid grid-cols-2 gap-3 text-xs">
          <div>
            <span className="text-slate-400 block text-[10px] uppercase font-semibold">Location</span>
            <span className="font-medium text-slate-700 flex items-center gap-1 mt-0.5 truncate">
              <MapPin size={11} className="text-slate-400 shrink-0" />
              {supplier.location}
            </span>
          </div>
          <div>
            <span className="text-slate-400 block text-[10px] uppercase font-semibold">Fulfillment Rate</span>
            <span className="font-bold text-emerald-700 flex items-center gap-1 mt-0.5">
              <CheckCircle2 size={11} className="text-emerald-600 shrink-0" />
              {supplier.fulfillmentRate}%
            </span>
          </div>
          <div>
            <span className="text-slate-400 block text-[10px] uppercase font-semibold">Min Order Value</span>
            <span className="font-semibold text-slate-900 mt-0.5 block">{supplier.minOrderValue}</span>
          </div>
          <div>
            <span className="text-slate-400 block text-[10px] uppercase font-semibold">Lead Time</span>
            <span className="font-medium text-slate-700 flex items-center gap-1 mt-0.5">
              <Clock size={11} className="text-slate-400 shrink-0" />
              {supplier.leadTime}
            </span>
          </div>
        </div>

        <div className="mt-4 pt-3 border-t border-slate-100 flex items-center justify-between text-[11px] text-slate-500">
          <span className="font-mono bg-slate-50 px-2 py-0.5 rounded border border-slate-200">
            {supplier.nafdacLicense}
          </span>
          <span className="flex items-center gap-1 text-blue-800 font-semibold">
            <Package size={12} />
            {supplier.productCount} SKUs
          </span>
        </div>
      </div>

      <div className="mt-5 pt-3 flex items-center gap-2">
        <button
          onClick={() => {}}
          className="flex-1 py-2 px-3 rounded-xl bg-linear-to-r from-[#1e40af] via-[#0284c7] to-[#00b87c] hover:from-[#1d4ed8] hover:to-[#059669] text-white text-xs font-bold transition-all text-center shadow-xs cursor-pointer"
        >
          Compare Quotes
        </button>
        <button
          onClick={() => {}}
          className="py-2 px-3 rounded-xl border border-slate-200 hover:bg-slate-50 text-slate-700 text-xs font-semibold transition-colors cursor-pointer"
        >
          Direct RFQ
        </button>
      </div>
    </div>
  );
};
